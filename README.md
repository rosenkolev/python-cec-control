# CEC Rockchip

## Development

```shell
python3 -m venv .venv
source .venv/bin/activate
pip3 install setuptools pybind11
# register paths
python -m pybind11 --includes
```

run 

```
pip install .
```